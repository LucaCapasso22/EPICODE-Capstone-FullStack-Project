# Configurazione Database
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/rnbmx_shop}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:1hhPgsa127}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# JWT Configurazione
jwt.secret=${JWT_SECRET:RN_BMX_SHOP_SECRET_KEY}
jwt.expiration=86400000

# Stripe Configurazione
stripe.api.key=${STRIPE_API_KEY:sk_test_placeholder}

# Server port - porta alternativa per evitare conflitti
server.port=8081

# JWT Properties
bmx.app.jwtSecret=${JWT_SECRET:RN_BMX_SHOP_SECRET_KEY}
bmx.app.jwtExpirationMs=86400000 

# Logging per debug
logging.level.root=INFO
logging.level.com.rnbmx.shop=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate=INFO 